{
  "framework": "nextjs",
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff HEAD~1 --name-only | grep -qE '^(src/|next\\.config|package\\.json|vercel\\.json|public/)' || exit 0"
}
